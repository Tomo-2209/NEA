# Testing Table

Each test below was carried out on the finished program. Tests are grouped where multiple related success criteria can be verified by a single action. The "Success Criteria" column lists the criterion number(s) from `success_criteria.md` that each test covers.

---

| Test No. | Success Criteria | Description | Expected Outcome | Observed Outcome | Remedial Action |
|----------|-----------------|-------------|-----------------|-----------------|-----------------|
| T01 | 11, 12 | Launch the application and observe the first dialog that appears. | A difficulty-selection dialog appears before the game board is visible. It presents exactly two labelled buttons: "GCSE" and "A-Level". | On launch, a dialog titled "Tic-Tac-Total – Difficulty" appeared immediately, before the game board was shown. It displayed exactly two option buttons labelled "GCSE" and "A-Level". | None |
| T02 | 13, 14 | After selecting GCSE difficulty, observe the player-name dialogues. Then cancel (dismiss) both dialogues to test default-name fallback. | Two successive name-entry dialogs appear, one for Player 1 and one for Player 2. Cancelling or leaving the field blank results in the default names "Player 1" and "Player 2" being used. | Two input dialogs appeared in sequence. When both were dismissed without entering text, the game started with players named "Player 1" and "Player 2" as shown in the statistics panel and turn label. | None |
| T03 | 17 | After completing the start-up dialogs, inspect the main application window. | The window title bar reads "Tic-Tac-Total". | The title bar read "Tic-Tac-Total". | None |
| T04 | 18, 19, 20, 21, 22 | Observe the overall layout of the main window once the game starts. | The window contains five regions: header at the top (bold white text on dark background), statistics panel on the left, game board in the centre, Reset button at the bottom (dark background, legible text), and a vertical grid-size slider on the right. | All five regions were present and correctly positioned. The header displayed centred bold white text on a dark grey background. The Reset button had dark background and was appropriately labelled. The slider was vertical and appeared on the right-hand side. The statistics panel was permanently visible on the left. | None |
| T05 | 23, 27, 29 | Start the game and examine the initial board state. | The game board displays a 3×3 grid (the default size). All 9 tiles appear as blank white buttons with no text. | On first launch a 3×3 grid was displayed. All tiles were blank white with no symbols or text. | None |
| T06 | 24, 25, 26 | Move the grid-size slider to each value between 3 and 10 and observe the result. | For every integer position from 3 to 10, the board immediately rebuilds as a square grid of that size and a new round begins. No sizes outside this range are selectable. | Moving the slider through all positions (3, 4, 5, 6, 7, 8, 9, 10) caused the board to rebuild at the corresponding size each time. The slider stops at 3 (minimum) and 10 (maximum). A new round began immediately at each new size. | None |
| T07 | 28 | Set the grid size to 10×10 and observe the tile symbols. | The "X" and "O" symbols on claimed tiles remain clearly legible — font size scales down automatically so that symbols are not clipped or too small to read. | Symbols on a 10×10 grid were smaller than on a 3×3 grid but remained fully legible and were correctly centred within each tile. No clipping or truncation was observed. | None |
| T08 | 30, 31 | Click an empty standard tile on a 3×3 board. Answer the maths question correctly. Then attempt to click the same (now occupied) tile again. | After a correct answer the clicked tile displays the current player's symbol (X or O) centred in the tile. The second click on the occupied tile has no effect. | The symbol appeared correctly in the tile after a correct answer. Clicking the occupied tile again produced no visible change and the turn label remained unchanged. | None |
| T09 | 33, 34 | Click an empty standard tile and answer the maths question correctly. | A maths question dialog appears immediately on clicking the tile. On submitting a correct answer, the tile flashes green and the symbol is placed. | The maths question dialog appeared instantly on tile click. After entering the correct answer, the tile turned green. The symbol was visible in the tile. | None |
| T10 | 35, 36 | Click an empty standard tile and deliberately enter a wrong answer to the maths question. | The tile flashes red for approximately 1 500 milliseconds, displaying the correct answer during the flash. After the flash, the turn passes to the other player and the turn label updates accordingly. | The tile turned red and displayed the correct answer text. After the flash cleared, the turn label updated to show the other player's name followed by "'s turn". | None |
| T11 | 32, 37 | Click a tile, answer correctly, and verify the win-check occurs before the turn changes. While the green flash is running, attempt to click another tile. | After a correct answer, the program checks for a win before switching turns. Clicks on other tiles during the green flash are ignored. | After placing a symbol, the program immediately ran a win check (evidenced by the winning animation triggering when a line was completed). Clicking other tiles during the flash had no effect. | None |
| T12 | 38 | Play several rounds at GCSE difficulty and record the topics of standard questions that appear. | Over multiple questions, all seven GCSE standard topics appear: linear equations, percentages, area of rectangles/triangles, mean of a dataset, powers and indices, algebraic substitution, and speed/distance/time. | All seven question types were observed across a session of approximately 30 questions at GCSE level. | None |
| T13 | 39 | Play several rounds at A-Level difficulty and record the topics of standard questions that appear. | Over multiple questions, all seven A-Level standard topics appear: differentiation (power rule), second derivatives, stationary points, definite integration, logarithms, binomial coefficients, and inverse-trigonometric angles. | All seven A-Level question types were observed across a session of approximately 30 questions at A-Level level. | None |
| T14 | 40 | Answer the same type of question (e.g. linear equation) twice in the same session and compare the numerical values. | The numbers in the two questions differ — parameters are freshly randomised each time even when the topic type repeats. | Two consecutive linear-equation questions were generated with different coefficients and constant terms, confirming that numerical parameters are re-randomised on each generation. | None |
| T15 | 41 | Enter the correct answer to a question with leading/trailing spaces and mixed case (where the answer is a string). | The answer is accepted despite the extra whitespace and differing letter case. | Testing an answer like " 42 " (with spaces) and an answer typed in the wrong case for a string response were both accepted as correct. | None |
| T16 | 42 | Inspect a question that has more than one valid answer form (e.g. "0.5" or "1/2"). Enter each accepted form in separate attempts. | Both alternative forms are accepted as correct. | For a relevant question in the GCSE bank, both "0.5" and "1/2" were accepted as correct answers when entered separately. | None |
| T17 | 43, 44 | Start a game on a 5×5 grid (which has 3 bomb tiles). Observe all tiles before clicking. Then click a bomb tile. | Before being clicked, bomb tiles look identical to empty standard tiles — completely blank and white. Clicking one reveals the bomb graphic and begins the fuse-burn animation on the tile. | All tiles appeared identical before any were clicked. Clicking a bomb tile caused it to reveal a bomb graphic and begin its animated fuse-burn sequence. The tile's background changed to show the animation. | None |
| T18 | 45, 46, 47 | Click a bomb tile and observe the dialog that appears. | Simultaneously with the bomb animation beginning, a bomb-diffuse dialogue appears. It shows the harder maths question text, an answer text field, a "DEFUSE!" submit button, a countdown label starting at "35s", and a progress bar. | The diffuse dialog appeared at the same time as the bomb animation began. It contained the question, an input field, the "DEFUSE!" button, and a countdown starting at 35 seconds with a visible orange progress bar. | None |
| T19 | 48 | In the bomb-diffuse dialog, wait until the countdown reaches 5 seconds and observe the countdown label colour. | The countdown label changes from yellow to red when 5 or fewer seconds remain. | The label was yellow at 35 s and changed to red at exactly 5 s remaining. | None |
| T20 | 49 | At GCSE difficulty, observe several bomb-diffuse questions across multiple rounds. | All four harder GCSE bomb-diffuse topics appear: quadratic equations, Pythagoras' theorem, simultaneous equations, and nth-term sequences. | All four harder GCSE topics were observed across multiple bomb clicks in a GCSE session. | None |
| T21 | 50 | At A-Level difficulty, observe several bomb-diffuse questions across multiple rounds. | All three harder A-Level bomb-diffuse topics appear: geometric series sums, chain-rule differentiation, and harder definite integrals. | All three harder A-Level topics were observed across multiple bomb clicks in an A-Level session. | None |
| T22 | 51, 52 | Click a bomb tile and answer the diffuse question correctly. | The bomb animation stops, the bomb tile converts to a blank standard tile, and the current player's symbol is placed in that cell. A win/draw check is then run. | After a correct diffuse answer, the bomb graphic disappeared and the tile became a normal white tile containing the player's symbol. The win check ran immediately after (evidenced by a win being detected if that cell completed a line). | None |
| T23 | 53, 54, 55 | Click a bomb tile and deliberately give a wrong answer (or allow the timer to expire). Observe the explosion and its aftermath. | The bomb detonates — an explosion particle animation plays. Once the animation completes, all tiles in the 3×3 area surrounding the bomb cell (including the bomb cell itself) are cleared of any symbols, and empty standard tiles replace them. | After a wrong answer, the explosion animation played. Upon completion, the 3×3 area centred on the bomb was fully cleared: all surrounding symbols were removed and all cells (including the bomb cell) became blank standard tiles. | None |
| T24 | 57 | Trigger a bomb explosion and attempt to click any tile during the animation. | All tile clicks are completely blocked while the explosion animation is running. No clicks are processed until the animation fully completes. | Clicking various tiles during the explosion animation had no effect. Once the animation finished and the turn label updated, tile clicks were accepted normally. | None |
| T25 | 58 | Trigger a bomb explosion (by failing to diffuse) and observe whose turn it is after the animation completes. | The turn passes to the other player once the explosion animation finishes. The turn label updates to show the new active player. | After the explosion animation ended, the turn label changed to display the other player's name followed by "'s turn". | None |
| T26 | 59, 61 | Start a new round on a 5×5 grid and inspect the board for bomb placement. Reset multiple times and observe new placements. | Bomb tiles are placed at random positions on the grid at the start of every round. No two bombs share the same cell. Repeated resets show different placements. | On each reset, bomb positions changed. Running the game multiple times confirmed no duplicate bomb positions within the same round. | None |
| T27 | 62, 63 | On a 3×3 grid, place three X symbols in a row. On a 5×5 grid, place four X symbols in a row. On a 7×7 grid, place five X symbols in a row. On a 9×9 grid, place six X symbols in a row. | A win is detected in each case at the exact required line length (3, 4, 5, 6 respectively), and not before. | Wins were detected at the correct line lengths for each grid size. Placing one fewer symbol in a line did not trigger a win. | None |
| T28 | 62 | Verify that wins are detected in all directions: a row, a column, and both diagonals (top-left to bottom-right, and top-right to bottom-left). | A win is detected and announced for a completing move regardless of whether the line is horizontal, vertical, or diagonal. | Wins were detected and celebrated for lines in all four directions tested. | None |
| T29 | 64, 65, 66 | Complete a winning line and observe the visual and label feedback. Then attempt to click a tile after the win is declared. | The tiles forming the winning line flash gold. The header label displays "[Winner's name] wins!". All tile clicks are ignored after the win. | After completing a line, the winning tiles flashed gold repeatedly. The header showed the correct winner message. Clicking any tile afterwards had no effect. | None |
| T30 | 67, 68 | On a 3×3 grid, fill every cell without completing a line (create a draw). | When all 9 cells are filled and no winner has been found, the game records a draw and the header displays a draw message (e.g. "It's a draw!"). | After filling all tiles on a 3×3 grid with no winning line, the header changed to display "It's a draw!". | None |
| T31 | 69, 70 | Reset the game 5 times and record which player goes first each time. | The starting player is chosen at random — over multiple rounds, both Player X and Player O are selected as the first player. The header displays "[Player name]'s turn" at the start of each round. | Over 10 resets, Player X started first 6 times and Player O started first 4 times, confirming random selection. The turn label always updated immediately on round start. | None |
| T32 | 71 | Answer a standard tile question correctly. Verify that after the win check (assuming no win), the turn switches. | After a correct answer, if no win or draw occurs, the turn label changes to the other player's name followed by "'s turn". | After placing a symbol with a correct answer and no win being triggered, the turn label updated to show the other player's name. | None |
| T33 | 72 | Answer a standard tile question incorrectly. Verify the turn switches after the red flash. | After red flash completes, the turn label changes to the other player's name followed by "'s turn". | After the red flash ended, the turn label updated to show the other player's name. | None |
| T34 | 73, 74 | Verify turn-switching after both bomb outcomes. First diffuse a bomb (correct answer) and confirm turn switches if no win. Then fail a bomb (wrong answer) and confirm turn switches after the explosion animation. | After a successful diffuse (with no win), the turn passes to the other player. After a failed diffuse, the turn passes to the other player once the explosion animation completes. | Both outcomes were verified: after a diffuse the turn label updated to the other player; after an explosion the turn label updated only once the animation had fully completed. | None |
| T35 | 75, 76, 77 | Win two rounds in a row as the same player. Check the score, win streak, and best streak in the statistics panel after each win. | The winning player's score increments by 1 per win. The win streak increments by 1 per consecutive win. The best streak equals or exceeds the current streak and never decreases. | After two consecutive wins for Player X, the statistics panel showed: Score = 2, Streak = 2, Best = 2. After Player O won the next round, Player X's streak reset to 0 but Best remained at 2. | None |
| T36 | 78, 79, 80, 81 | Answer several maths questions correctly and incorrectly across both players. Observe the statistics panel after each answer. | The statistics panel updates in real time after every answered question. The correct-answers count increments when a correct answer is given; the wrong-answers count increments on incorrect answers or expired bomb timers. All five statistics are visible for both players. | The stats panel updated after every question — correct and wrong counts changed immediately. All five statistics (wins, streak, best streak, correct, wrong) were displayed for both players throughout the session. | None |
| T37 | 83, 84, 85 | Press the Reset button during an active game. Observe the board state, which player goes first, and whether statistics are preserved. | The board clears immediately and a new round begins at the same grid size with a randomly chosen starting player. Player scores, streaks, and answer counts remain unchanged. | Pressing Reset cleared the board and started a new round. The grid size was unchanged. The starting player changed on some resets (random). All statistics in the side panel were preserved from before the reset. | None |

